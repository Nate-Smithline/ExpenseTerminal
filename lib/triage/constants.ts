/** Vendor qualifies for "make it a rule" when seen this many times already tagged. */
export const RULE_SEEN_THRESHOLD = 4;

/** Auto-dismiss rule offer (defaults to "just this one") after this many ms. */
export const RULE_OFFER_TIMEOUT_MS = 8000;
